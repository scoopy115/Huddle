# Browser dev mode (optional): run the engine with a fixed port/token and open http://localhost:1420
#   cd engine && HUDDLE_DEV_CORS=1 HUDDLE_TOKEN=devtoken .venv/bin/python -m huddle_engine serve --port 48731
VITE_ENGINE_URL=http://127.0.0.1:48731
VITE_ENGINE_TOKEN=devtoken
